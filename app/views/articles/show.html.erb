<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>title</th>
      <td><%= @article.title %></td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @article.content %></td>
    </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_employee_article_path(@article) %> |
<%= link_to 'Back', new_employee_article_path %>
